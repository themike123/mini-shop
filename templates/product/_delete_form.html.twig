<form class="form-delete" method="post" action="{{ path('product_delete', {'id': product.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn delete btn-danger">Eliminar</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Edit Sale{% endblock %}

{% block body %}
    <a href="{{ path('sale_index') }}" class="btn btn-info">Regresar</a>
    <br><br>
    <h1>Edit Sale</h1>

    {{ include('sale/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('sale/_delete_form.html.twig') }}

{% endblock %}
